"use client"

/**
 * @deprecated Prefer `<ShopifyAppTitleBar />` for native admin chrome.
 * Do not strip or CSS-hide `ui-title-bar` — that removes the overflow menu.
 */
export function ShopifyEmbeddedChromeFix() {
  return null
}
