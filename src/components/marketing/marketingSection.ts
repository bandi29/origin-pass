/** Marketing card — matches global `surfaces` rhythm */
export const marketingSection = {
  card:
    "group rounded-2xl border border-border bg-white/90 p-6 shadow-sm backdrop-blur-sm transition-all duration-200 ease-smooth hover:-translate-y-1 hover:shadow-lg",
} as const
